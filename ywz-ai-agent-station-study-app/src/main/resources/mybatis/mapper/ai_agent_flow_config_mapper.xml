<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ywzai.infrastructure.dao.IAiAgentFlowConfigDao">

    <resultMap id="AiAgentFlowConfigMap" type="com.ywzai.infrastructure.dao.po.AiAgentFlowConfig">
        <id column="id" property="id"/>
        <result column="agent_id" property="agentId"/>
        <result column="client_id" property="clientId"/>
        <result column="client_name" property="clientName"/>
        <result column="client_type" property="clientType"/>
        <result column="sequence" property="sequence"/>
        <result column="step_prompt" property="stepPrompt"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.ywzai.infrastructure.dao.po.AiAgentFlowConfig">
        INSERT INTO ai_agent_flow_config(agent_id, client_id, client_name, client_type, step_prompt, sequence, status,
                                         create_time)
        VALUES (#{agentId}, #{clientId}, #{clientName}, #{clientType}, #{stepPrompt}, #{sequence}, #{status}, NOW())
    </insert>
    <update id="update">
        update ai_agent_flow_config
        set
        agent_id = #{agentId},
        client_id = #{clientId},
        client_name = #{clientName},
        client_type = #{clientType},
        step_prompt = #{stepPrompt},
        sequence = #{sequence},
        status = #{status},
        create_time = NOW()
        <where>
            agent_id = #{agentId} and
            client_id = #{clientId} and
            client_name = #{clientName} and
            client_type = #{clientType} and
            step_prompt = #{stepPrompt} and
            status = #{status} and
            sequence = #{sequence}
        </where>
    </update>


    <select id="queryByAgentId" parameterType="java.lang.String" resultMap="AiAgentFlowConfigMap">
        SELECT id,
               agent_id,
               client_id,
               client_name,
               client_type,
               ai_agent_flow_config.step_prompt,
               sequence,
               status,
               create_time
        FROM ai_agent_flow_config
        WHERE agent_id = #{agentId}
        ORDER BY sequence ASC
    </select>

    <select id="queryByAgentIdAndClientId" resultMap="AiAgentFlowConfigMap">
        SELECT id,
               agent_id,
               client_id,
               client_name,
               client_type,
               ai_agent_flow_config.step_prompt,
               sequence,
               status,
               create_time
        FROM ai_agent_flow_config
        WHERE agent_id = #{agentId}
          AND client_id = #{clientId}
    </select>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE
        FROM ai_agent_flow_config
        WHERE id = #{id}
    </delete>
    <delete id="deleteByAgentId" parameterType="java.lang.String">
        DELETE
        FROM ai_agent_flow_config
        WHERE agent_id = #{agentId}
    </delete>


</mapper>