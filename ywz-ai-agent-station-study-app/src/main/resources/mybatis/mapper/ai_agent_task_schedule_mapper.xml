<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ywzai.infrastructure.dao.IAiAgentTaskScheduleDao">

    <resultMap id="AiAgentTaskScheduleMap" type="com.ywzai.infrastructure.dao.po.AiAgentTaskSchedule">
        <id column="id" property="id"/>
        <result column="agent_id" property="agentId"/>
        <result column="task_name" property="taskName"/>
        <result column="description" property="description"/>
        <result column="cron_expression" property="cronExpression"/>
        <result column="task_param" property="taskParam"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.ywzai.infrastructure.dao.po.AiAgentTaskSchedule">
        INSERT INTO ai_agent_task_schedule(agent_id, task_name, description, cron_expression, task_param, status, create_time, update_time)
        VALUES(#{agentId}, #{taskName}, #{description}, #{cronExpression}, #{taskParam}, #{status}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.ywzai.infrastructure.dao.po.AiAgentTaskSchedule">
        UPDATE ai_agent_task_schedule SET
            task_name = #{taskName},
            description = #{description},
            cron_expression = #{cronExpression},
            task_param = #{taskParam},
            status = #{status},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <select id="queryByAgentId" parameterType="java.lang.Long" resultMap="AiAgentTaskScheduleMap">
        SELECT id, agent_id, task_name, description, cron_expression, task_param, status, create_time, update_time
        FROM ai_agent_task_schedule
        WHERE agent_id = #{agentId}
        ORDER BY create_time DESC
    </select>

    <select id="queryAllEnabled" resultMap="AiAgentTaskScheduleMap">
        SELECT id, agent_id, task_name, description, cron_expression, task_param, status, create_time, update_time
        FROM ai_agent_task_schedule
        WHERE status = 1
        ORDER BY create_time DESC
    </select>

    <select id="queryById" parameterType="java.lang.Long" resultMap="AiAgentTaskScheduleMap">
        SELECT id, agent_id, task_name, description, cron_expression, task_param, status, create_time, update_time
        FROM ai_agent_task_schedule
        WHERE id = #{id}
    </select>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM ai_agent_task_schedule WHERE id = #{id}
    </delete>

</mapper>